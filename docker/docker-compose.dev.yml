version: '3.7'

services:
  traefik:
    ports:
      - 2015:80
      - 2080:8080
  app:
    environment:
      - N8N_SECURE_COOKIE=false
    volumes:
      - ../public:/app/public
      - ../package.json:/app/package.json
      - ../next.config.js:/app/next.config.js
      - ../prisma:/app/prisma
      - ../src:/app/src
      - ../server:/app/server
    command: 'npm run dev:n8n'
    ports:
      - 3000:3000
      - 4000:4000
      - 5678:5678

  supabase:
    ports:
      - 5432:5432
  mailserver:
    environment:
      - DEFAULT_RELAY_HOST=${MAIL_RELAY_HOST:-"[mailhog]:1025"}
    ports:
      - 25:25
      - 465:465
      - 587:587
      - 143:143
      - 993:993

  mailhog:
    ports:
      - 8025:8025
      - 1025:1025

  world3d:
    volumes:
      - ./world3d/src:/app/src
      - ./world3d/package.json:/app/package.json
      - ./world3d/package-lock.json:/app/package-lock.json
    command: 'npm run dev'
    ports:
      - 4100:4100

  llama:
    ports:
      - 8080:8080
